# Default values for component-executor.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
namespace: executors

image:
  service:
    repository: ""
    pullPolicy: IfNotPresent
    imageName: component-executor-production
    tag: 

app:
  service: component-executor

replicas:
  service: 5

environmentVariables:
  # - name: ACCESS_LIBRARY_VERSION
  #   value: 0.2.0
  # - name: CLOUD_LIBRARY_VERSION
  #   value: 0.2.0
  # - name: COMPONENT_LIBRARY_VERSION
  #   value: 0.2.0
  # - name: DATA_LIBRARY_VERSION
  #   value: 0.2.0
  # - name: DATAOP_LIBRARY_VERSION
  #   value: 0.2.0
  # - name: FACET_LIBRARY_VERSION
  #   value: 0.2.2
  # - name: MODEL_LIBRARY_VERSION
  #   value: 0.2.6
  # - name: NETWORK_LIBRARY_VERSION
  #   value: 0.2.0
  # - name: PROTO_LIBRARY_VERSION
  #   value: 0.2.0

imagePullSecrets: []

podAnnotations: {}

podSecurityContext: {}

securityContext: {}

service:
  service:
    port: 16107

resources:
  service:
    requests:
      memory: "512Mi"
      cpu: "0.5"
    limits:
      memory: "8Gi"
      cpu: "2"

nodeSelector:
  role: service

tolerations: []

affinity: {}

clusterId:

clusterSize:

useKubeEnvironment:
configMountPath: /etc/config